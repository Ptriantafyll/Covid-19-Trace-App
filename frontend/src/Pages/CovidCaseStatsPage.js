// import axios from "axios";

function CovidCaseStatsPage() {
  // const BaseURL = "http://localhost:3000/"; // api url
  // const [allUsersLastWeekVisits, setAllUsersLastWeekVisits] = useState();
  // const [currentUserLastWeekVisits, setCurrentUserLastWeekVisits] = useState();

  // function getLastWeeksVisitsOfAllUsers() {
  //   axios
  //     .get(BaseURL + "visit/lastweek/allusers")
  //     .then((response) => {
  //       // console.log(response.data.visits_of_user);
  //       const uservisits = response.data.lastweeks_visits;

  //       for (const visit in uservisits) {
  //         console.log("in allusers: " + uservisits[visit].time);
  //       }
  //       // return uservisits;
  //     })
  //     .catch((error) => {
  //       console.log(error.response.data.error.message); // message sent from backend
  //     });
  // }

  // function getLastWeeksVisitsOfCurrentUser() {
  //   axios
  //     .get(BaseURL + "visit/lastweek/" + user_context.username)
  //     .then((response) => {
  //       // console.log(response.data.visits_of_user);
  //       const uservisits = response.data.lastweeks_visits;

  //       for (const visit in uservisits) {
  //         console.log(user_context.username + ": " + uservisits[visit].time);
  //       }
  //       // return uservisits;
  //     })
  //     .catch((error) => {
  //       console.log(error.response.data.error.message); // message sent from backend
  //     });
  // }

  // getLastWeeksVisitsOfCurrentUser();

  // getLastWeeksVisitsOfAllUsers();

  // console.log(allUsersLastWeekVisits);

  // console.log(currentUserLastWeekVisits);

  return <div>hello</div>;
}

export default CovidCaseStatsPage;
